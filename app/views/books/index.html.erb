<%= render 'users/info', user: @user %>
<%= render 'books/book1',book: @book %>
<h2>Books</h2>
<%= @books.each do |book| %>
<table>
 <tbody>
     <tr>
         <th>Title</th>
         <th><%= book.title %></th>
     </tr>
     <tr>
         <th>Opinion</th>
         <th><%= book.opinion %></th>
     </tr>
     
 </tbody>
</table>
<% end %>
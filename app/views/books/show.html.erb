<%= render 'users/info', user: @user %>
<%= render 'books/book1',book: @booknew %>
<h1>Book detail</h1>
<table>
 <thead>
  <tr>
   <th>Title</th>
  <th>Opinion</th>
  </tr>
 </thead>
 <tbody>
      <tr>
         <td><%= @book.title %></td>
         <td><%= @book.body %></td>
         <td><%= link_to "Edit", edit_book_path(@book.id)%></td>
         <td><%= link_to "Delete", book_path(@book.id),method: :delete, "data-confirm" => "本当に削除しますか？"%></td>
     </tr>
   </tbody>
  
</table>

